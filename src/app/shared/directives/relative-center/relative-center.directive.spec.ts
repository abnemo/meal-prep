import { RelativeCenterDirective } from './relative-center.directive';

describe('RelativeCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new RelativeCenterDirective();
    expect(directive).toBeTruthy();
  });
});
